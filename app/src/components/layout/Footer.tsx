import type { FC } from 'react';
import styles from '../../styles/Footer.module.scss';

const Footer: FC = () => (
  <footer className={styles.footer}>Copyright &copy; 2022 Brian Reece</footer>
);

export default Footer;
